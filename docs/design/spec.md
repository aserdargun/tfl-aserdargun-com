# TFL visual implementation contract

Reference: concept.png, generated with built-in Image Gen, 1536 × 1024. User requested immediate implementation; no separate concept review gate.

Charcoal #101719 background, #151e22 surfaces, #344247 rules, #e6edf0 text, #a4b2b9 secondary text, mint #79deba active/decode and amber #dfb45e prefill. Geist 400/500/600/700; IBM Plex Mono for quantities and identities. 4–6px corners, 8/12/16/24px spacing, 1px rules. Lucide 16px, 1.75px strokes for transport/navigation.

Header identity and ecosystem links; modest two-line hero and prompt submission; six mode tabs; 3:1 world/inspector split; queue → scheduler → compute with stream and KV below; playback rail; Token Flow 101; scientific boundary footer. Native semantic HTML draws the system because all geometry encodes state. No bitmap is shipped as a working interface.

Required functional extensions: scenario controls, experiment comparisons, resource view, stack disclosure, guided lesson navigation, EN/TR, export. Extend the same rails, typography and panel families. Mobile collapses to one system column with controls in a bottom disclosure; inspector follows the world.

Intentional scientific corrections to generated concept: use synthetic model profiles (never Llama hardware estimates), label scheduler policies '-like', derive all numbers/events, do not place one request in multiple lifecycle states, show actual KV allocation separately from conservative reservation, describe chunked prefill without an 'all at once' assertion. Empty default world until SEND is required for the actual lifecycle.

Allowed first viewport copy comes from user brief: TFL, Token Flow Laboratory, tagline, One model. Many requests. Finite resources., Educational Simulation, Follow/Load/Memory/Scheduler/Metrics/Labs, Send, request queue/scheduler/prefill/decode/stream/KV, inspector, Play/Pause/Step/Reset/Speed, Token Flow 101. Additional labels only to explain functioning controls and scientific assumptions.
